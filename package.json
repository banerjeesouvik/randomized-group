{
  "name": "randomized-group",
  "version": "0.0.3",
  "description": "Function takes two arguments, first one is an array(names) and second one is a number(n). This function returns an array of size n where each element is a random subset of names. ",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/banerjeesouvik/randomized-group.git"
  },
  "author": "Souvik Banerjee",
  "license": "MIT" 
}
